# SMF_IMAGE_NAME: "smf"
# SMF_IMAGE_TAG: "2.1.7"

# Absolute path on your computer to the file created by `docker image save`.
# SMF_IMAGE_TAR: "/path/to/smf-2.1.7.tar"

# Unencrypted test only: do not reuse these keys in production.
# The image, archive, and their pins are defined in vars/smf-build.yml.
# They must be at least 24 characters long.
SMF_DB_PASSWORD: "smf-prueba-clave-2026-local"
SMF_MYSQL_ROOT_PASSWORD: "mysql-root-prueba-2026-local"